sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t RishiCghs/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true RishiCghs/docker-react npm run test
